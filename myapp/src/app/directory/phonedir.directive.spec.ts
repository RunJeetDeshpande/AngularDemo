import { PhonedirDirective } from './phonedir.directive';

describe('PhonedirDirective', () => {
  it('should create an instance', () => {
    const directive = new PhonedirDirective();
    expect(directive).toBeTruthy();
  });
});
